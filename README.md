# window创建python虚拟环境指南 / Guide to Creating a Python Virtual Environment in Windows
需要先用vscode打开目标文件夹。
然后运行：
D:\Python313\python -m venv venv1
即可创建一个名字为venv1的虚拟环境 (D:\Python313\为python环境的安装路径)。
输入
.\venv1\Scripts\activate
可以激活该虚拟环境。
输入
deactivate
可以退出虚拟环境。

